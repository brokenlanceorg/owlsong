create table OPTIONS (
   stock_symbol      varchar( 6 ),
   option_delta      double,
   stock_delta       double,
   option_depth      tinyint,
   days_exipre       smallint,
   in_money          boolean
);
